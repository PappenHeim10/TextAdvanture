import screen.*; // Importiere die Window-Klasse aus dem screen-Paket


class MyProg {
    public static void main(String[] args) {

        // Neues Fenster erstellen
        Window window = new Window();
        window.init(); // Initialisiere das Fenster
    }
}